% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/embeddings.R
\name{lex_neighbors}
\alias{lex_neighbors}
\title{Find nearest embedding neighbors for a target word}
\usage{
lex_neighbors(word, n = 10, candidates = NULL, model = c("2024", "2014"))
}
\arguments{
\item{word}{character. A single target word.}

\item{n}{integer. Number of nearest neighbors to return (default 10).
The target word itself is excluded from results.}

\item{candidates}{character vector or NULL. If not NULL, restrict the search
to this set of words. Useful for finding neighbors among normed words only
(e.g., \code{candidates = lexis_wide$word}).}

\item{model}{character. Which GloVe release to use: \code{"2024"} (default) or
\code{"2014"}.}
}
\value{
A tibble with columns: word, similarity (cosine, higher = more
  similar). Sorted descending by similarity.
}
\description{
Computes cosine similarity between a target word's GloVe vector and all
other words in the selected GloVe matrix (or a subset), returning the top-N
most similar words.
}
\examples{
lex_neighbors("happy")
lex_neighbors("run", n = 20, candidates = lexis_wide$word)

}
