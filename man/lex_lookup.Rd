% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lookup.R
\name{lex_lookup}
\alias{lex_lookup}
\title{Look up psycholinguistic norms for one or more words}
\usage{
lex_lookup(words, dims = NULL, wide = FALSE)
}
\arguments{
\item{words}{character vector. One or more words to look up (case-insensitive).}

\item{dims}{character vector or NULL. Dimensions to return. If NULL, all
dimensions are returned. See \code{lexis_meta$dimension} for valid names.}

\item{wide}{logical. If TRUE, pivot to wide format (one row per word).
Default FALSE (long format).}
}
\value{
A tibble. In long format: word, lemma, dataset, dimension, mean, sd,
  n_ratings, scale_min, scale_max. In wide format: word, lemma, then one
  column per dimension.
}
\description{
Returns a tidy (long) tibble of norm values for the requested words.
By default returns all available dimensions; use \code{dims} to restrict.
}
\examples{
lex_lookup("dog")
lex_lookup(c("dog", "cat"), dims = c("aoa", "concreteness", "valence"))
lex_lookup("run", wide = TRUE)

}
